{
  "name": "unfinishedproject",
  "version": "0.0.0",
  "description": "unfinishedproject-server",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build:live",
    "build": "tsc -p .",
    "build:live": "nodemon --exec \"ts-node\" src/index.ts"
  },
  "author": {
    "name": ""
  },
  "dependencies": {
    "ws": "^7.4.2"
  },
  "devDependencies": {
    "@types/node": "^14.14.20",
    "@types/ws": "^7.4.0",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  }
}
